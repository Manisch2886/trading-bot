# M3 — grep -n "EINGEFROREN\|SPERRLISTE_DATEIEN\|EINGEFROREN)" research/vorregistrierung/herkunft.py ; sed -n '50,80p;105,125p' research/vorregistrierung/herkunft.py  (gemessen 2026-09-22T17:09:49Z)

57:EINGEFROREN = [
66:SPERRLISTE_DATEIEN = [
114:    for rel in ([os.path.relpath(REGISTERDATEI, _HIER)] + EINGEFROREN):

## Listen woertlich
_HIER = os.path.dirname(os.path.abspath(__file__))
BASE_DIR = os.environ.get("TB30A_BASE_DIR") or os.path.dirname(
    os.path.dirname(_HIER))

PROTOKOLL = os.path.join(_HIER, "ergebnisse", "herkunft_protokoll.jsonl")

REGISTERDATEI = os.path.join(BASE_DIR, "docs", "VORREGISTRIERUNG_neuselektion.md")
EINGEFROREN = [
    "registerdaten.py", "faltenplan.py", "benchmark.py", "kennzahlen.py",
    "auswertung.py", "messgroessen.py", "pruefe_grenzsaetze.py",
    "ergebnisse/messgroessen.json", "ergebnisse/faltenplan.json",
    "ergebnisse/benchmark_drawdowns.json",
]
# Die Commit-Hashes, die zusaetzlich auf die Sperrliste gehoeren: Simulation,
# Erkennung und Optimierer je Bot. Sie liegen ausserhalb dieses Ordners und
# werden ueber den Git-Commit des Repos mit erfasst.
SPERRLISTE_DATEIEN = [
    "shared/messkette.py", "shared/zuteilung.py",
    "strategies/*/equity_simulation.py", "strategies/*/multi_symbol_optimise.py",
    "strategies/*/multi_symbol_walk_forward.py",
]


def _sha256_datei(pfad: str) -> str:
    h = hashlib.sha256()
    with open(pfad, "rb") as f:
        for block in iter(lambda: f.read(1 << 20), b""):
            h.update(block)
    return h.hexdigest()


...
        h.update(_sha256_datei(pfad).encode("utf-8"))
    return {"datenstand": h.hexdigest(), "dateien": len(dateien)}


def register() -> dict:
    """SHA-256 ueber die eingefrorenen Festlegungen."""
    h = hashlib.sha256()
    fehlend = []
    teile = []
    for rel in ([os.path.relpath(REGISTERDATEI, _HIER)] + EINGEFROREN):
        pfad = os.path.normpath(os.path.join(_HIER, rel))
        if not os.path.exists(pfad):
            fehlend.append(rel)
            continue
        d = _sha256_datei(pfad)
        teile.append({"datei": rel, "sha256": d})
        h.update(rel.encode("utf-8"))
        h.update(d.encode("utf-8"))
    return {"register": h.hexdigest(), "teile": teile, "fehlend": fehlend}



## Mengenvergleich EINGEFROREN gegen die Pfade der 14 Punkte (python3-Heredoc: ast.literal_eval der Liste Z. 57, Pfade aus dem Abbild 6a1b732e…, das die Sonde gegen Abschnitt 10 mit (ii)=0 prueft)
EINGEFROREN (auf Repo-Pfade aufgeloest): 10
Pfade der 14 Punkte laut Abbild 6a1b732e (Abschnitt 10): 10

in Abschnitt 10, fehlt in EINGEFROREN:
   config/sp500_top150.txt
   config/top25_symbols.txt
   research/vorregistrierung/herkunft.py
   shared/zuteilung.py

in EINGEFROREN, kein Pfad eines Punktes in Abschnitt 10:
   research/vorregistrierung/ergebnisse/messgroessen.json
   research/vorregistrierung/kennzahlen.py
   research/vorregistrierung/messgroessen.py
   research/vorregistrierung/pruefe_grenzsaetze.py

bestimmt (21.9/23.7), in EINGEFROREN: False
Schluessel des Abbilds: ['art', 'erzeugt', 'pfadregel', 'punkte', 'quelle']

## Weitere Tatsachen aus Fables Notiz, nachgemessen
### grep -rn 'SPERRLISTE_DATEIEN' --include=*.py . (ohne trading-env)
research/vorregistrierung/herkunft.py:66:SPERRLISTE_DATEIEN = [
shared/sperrlistensonde.py:70:`SPERRLISTE_DATEIEN` nicht (36.6: die sind nicht das Abbild). Und sie prueft
### grep -rn 'EINGEFROREN' --include=*.py research/vorregistrierung
research/vorregistrierung/herkunft.py:57:EINGEFROREN = [
research/vorregistrierung/herkunft.py:114:    for rel in ([os.path.relpath(REGISTERDATEI, _HIER)] + EINGEFROREN):
### grep -rln -E 'import herkunft|herkunft\.py' --include=*.py research/vorregistrierung (ohne herkunft.py selbst)
research/vorregistrierung/auswertung.py
research/vorregistrierung/beispieldaten.py
### grep -rn -E '\.block\(' --include=*.py research shared strategies
research/turn_of_month/auswertung.py:206:        "herkunft": hk.block("auswertung", daten_dir),
research/turn_of_month/staging.py:127:        "herkunft": hk.block("staging", daten_dir),
### Dateien herkunft.json / herkunft_protokoll.jsonl unter research/vorregistrierung
   (find-Ausgabe oben; leer = existieren nicht)
### sed -n '41p' research/vorregistrierung/auswertung.py
        mittlere_exposure
### Randbefund TB-84 nachgemessen: grep -n 'herkunft' research/etf_trendfolge/datenstand.py
19:Er kommt aus `research/vorregistrierung/herkunft.py::datenstand` - derselben
25:`herkunft.py` achtet auf `TB30A_BASE_DIR`. Der Selbsttest nutzt das, um die
43:herkunft = reg.lade_fremdes_modul(
44:    "herkunft", os.path.join(reg.VORREGISTRIERUNG_DIR, "herkunft.py"))
48:    """{'datenstand': ..., 'dateien': ...} - aus `herkunft.py`, nicht neu."""
49:    return herkunft.datenstand(daten_dir or reg.KURSDATEN_DIR_VERBOTEN)
### Nachprobe zu den zwei Treffern oben und zu 'Z. 41': grep -n 'herkunft' research/vorregistrierung/auswertung.py research/vorregistrierung/beispieldaten.py; grep -n -E '^(import|from) ' beider Dateien auf herkunft
research/vorregistrierung/auswertung.py:51:    <wurzel>/<bot>/herkunft.json
research/vorregistrierung/auswertung.py:52:        Commit-Hash, Datenstand-Hash, Register-Hash - siehe herkunft.py.
research/vorregistrierung/beispieldaten.py:142:    with open(os.path.join(ordner, "herkunft.json"), "w", encoding="utf-8") as f:
research/vorregistrierung/beispieldaten.py:145:                              "aus herkunft.py.",
   import/from mit herkunft: 0 Treffer

## Ergebnis M3
   Z. 57 EINGEFROREN (zehn Eintraege), Z. 66 SPERRLISTE_DATEIEN (fuenf Muster), Z. 114 Verwendung in register() — wie erwartet.
   SPERRLISTE_DATEIEN: ausser der Definition nur in einem Kommentar von shared/sperrlistensonde.py:70 genannt, nirgends gelesen — 'toter Code' bestaetigt.
   herkunft.py in research/vorregistrierung/ von keinem Modul importiert (die zwei Treffer sind Text: Docstring auswertung.py, Beispieldaten-Schreiber beispieldaten.py:142/145); herkunft*.json* existieren nicht; block() nur in research/turn_of_month/ (auswertung.py:206, staging.py:127) — bestaetigt.
   ⚠️ ABWEICHUNG 1: Der Datenvertrag nennt herkunft.json in auswertung.py Z. **51** (Z. 52 'siehe herkunft.py'), nicht Z. 41. Z. 41 ist '        mittlere_exposure'.
   ⚠️ ABWEICHUNG 2: 'an fuenf Stellen ab (fehlend: 3; zusaetzlich: 3)' — Fable zaehlt fuenf und nennt sechs. Der mechanische Mengenvergleich gegen die Pfade der 14 Punkte ergibt: in Abschnitt 10, nicht in EINGEFROREN: VIER (config/sp500_top150.txt, config/top25_symbols.txt, research/vorregistrierung/herkunft.py, shared/zuteilung.py), dazu der BESTIMMTE Pfad benchmark_drawdowns_vt.json (kein Punkt); in EINGEFROREN, kein Pfad eines Punktes: VIER (kennzahlen.py, messgroessen.py, pruefe_grenzsaetze.py, ergebnisse/messgroessen.json). Fables sechs Namen stimmen alle; es fehlen in seiner Aufzaehlung herkunft.py, shared/zuteilung.py und ergebnisse/messgroessen.json.
   Randbefund TB-84 bestaetigt: research/etf_trendfolge/datenstand.py:43-49 laedt herkunft.py und ruft datenstand() auf (ausserhalb des Laufbereichs der Vorregistrierung).
